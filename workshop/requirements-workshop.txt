# Python 3.10+ (na Replitu viz .replit → 3.11). Instalace: pip install -r requirements-workshop.txt
# Stejné verze jako requirements.txt (Replit často bere requirements.txt automaticky).
numpy>=1.24,<2.1
pandas>=2.0,<2.3
scikit-learn>=1.3,<1.6
matplotlib>=3.7,<4

# Volitelné: rychlejší kNN na větších datech (na některých systémech složitější instalace)
# faiss-cpu>=1.7.4
