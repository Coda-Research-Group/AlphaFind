# Reference – jen pro lektora

Soubory zde **nepatří** do studentského zipu (nebo je studentům nedávejte jako hotové řešení).

- `reseni_minimalni.py` – načtení dat, baseline Top‑k, K-means, bucket soubory, routed search, kosinové přehodnocení, PCA pomocná funkce.

Po spuštění `reseni_minimalni.py` jako skript se v `data/` vytvoří `bucket_0.csv` … (přepíše existující).
